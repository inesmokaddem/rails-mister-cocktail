<div class="container">
  <h1>Cocktails</h1>
  <% @cocktails.each do |cocktail| %>
  <%= link_to cocktail_path(cocktail) do %>
  <div class="card_cocktail">
    <h3><%= cocktail.name%></h3>
  </div>
  <% end %>
  <% end %>
</div>
